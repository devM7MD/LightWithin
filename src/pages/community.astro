---
import Layout from '../layouts/Community.astro';
import CommunityChat from '../components/Community.jsx';

---
<Layout>
    <div>
        <h1 class="text-white text-4xl">Not Ready yet!</h1>
    </div>
</Layout>
<style is:global>
    @keyframes float {
      0% {
        transform: translateY(0) translateX(0);
      }
      50% {
        transform: translateY(-20px) translateX(10px);
      }
      100% {
        transform: translateY(0) translateX(0);
      }
    }
  </style>